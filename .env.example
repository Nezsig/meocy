# Server Configuration
PORT=3000
NODE_ENV=production

# Admin dashboard login password (set a strong value in .env.local / Railway variables)
ADMIN_PASSWORD=change-me

# Resend email API key (used by /api/send-email)
RESEND_API_KEY=
